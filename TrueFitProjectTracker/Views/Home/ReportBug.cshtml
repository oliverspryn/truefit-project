@model TrueFitProjectTracker.Models.BugModel

@section styles{
       @Styles.Render("~/Content/Bug.css")
}
@Html.Partial("PartialViews/_Styles")
@{Layout = "";}
<script src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
<script>
    $(document).ready(function () {
        $(onsubmit).click(function () {
            @Url.Action("_ReportBug", "Home");


        });
    });
        </script>
@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Issue</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Summary)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Summary)
            @Html.ValidationMessageFor(model => model.Summary)
        </div>
        	
          @Html.HiddenFor(model => model.ProjectKey, new { isHidden = true })
        <div class="editor-label">
            @Html.LabelFor(model => model.Description)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Description)
            @Html.ValidationMessageFor(model => model.Description)
        </div>
        <br />
        <input id="filMyFile" type="file" runat="server">
        <br />
        <p>
            <input type="submit" class="btn btn-default" value="Create" />
        </p>
    </fieldset>
}
@*}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>*@
